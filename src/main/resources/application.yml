server:
  port: ${SERVER_PORT}
  error:
    include-stacktrace: always

security:
  jwt:
    issuer: com.paperstreetsoftware.auth
    public-key: classpath:jwt_public_key.der

pdf:
  resource-path: classpath:templates

spring:
  application:
    name: pdf-service

logging:
  level:
    web:
      DEBUG